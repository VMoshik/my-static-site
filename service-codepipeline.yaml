apiVersion: v1
kind: Service
metadata:
  name: caringup-demo-service
  namespace: demo
spec:
  type: LoadBalancer
  selector:
    app: caringup-demo
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
